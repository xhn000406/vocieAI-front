# Expo/EAS 登录问题排查

## 问题：输入密码后没有反应

这是常见问题，通常有以下几个原因和解决方案：

---

## 🔍 解决方案

### 方案1：使用浏览器登录（推荐）

Expo 支持浏览器登录，更稳定可靠：

```bash
# 使用浏览器登录
npx expo login --web

# 或
eas login --web
```

这会打开浏览器，在浏览器中完成登录，然后自动返回终端。

---

### 方案2：检查网络连接

如果网络无法访问 Expo 服务器，可以：

1. **检查网络**
   ```bash
   # 测试连接
   ping expo.dev
   ```

2. **使用代理（如果在国内）**
   ```bash
   # 设置代理
   export HTTP_PROXY=http://your-proxy:port
   export HTTPS_PROXY=http://your-proxy:port
   
   # 然后再登录
   eas login
   ```

---

### 方案3：使用访问令牌登录

如果你有访问令牌（Access Token）：

```bash
eas login --access-token YOUR_TOKEN
```

获取令牌：
1. 访问 https://expo.dev
2. 登录后进入 Settings -> Access Tokens
3. 创建新的访问令牌

---

### 方案4：等待响应

有时服务器响应较慢，可以：
- 等待 10-30 秒
- 如果还是没有反应，按 `Ctrl+C` 取消，然后重试

---

### 方案5：清除缓存后重试

```bash
# 清除 Expo 缓存
npx expo logout
npx expo login

# 或清除 npm 缓存
npm cache clean --force
```

---

## 🚀 替代方案：不使用 EAS Build

如果登录一直有问题，可以使用其他方式：

### 方式1：使用 Expo Go（最简单）

无需登录，直接使用：

```bash
cd frontend
npm start
# 扫描二维码即可
```

### 方式2：本地构建（无需登录）

**Android:**
```bash
cd frontend
npx expo prebuild
cd android
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

**iOS (macOS):**
```bash
cd frontend
npx expo prebuild
cd ios
pod install
open ios/YourApp.xcworkspace
# 在 Xcode 中运行
```

---

## 💡 推荐流程

1. **先尝试浏览器登录**
   ```bash
   npx expo login --web
   ```

2. **如果还是不行，使用 Expo Go 进行开发测试**
   ```bash
   npm start
   ```

3. **需要打包时，使用本地构建**
   ```bash
   npx expo prebuild
   ```

---

## 📝 注意事项

- 密码输入时**不会显示**，这是正常的安全行为
- 输入完密码后按 `Enter` 键
- 如果卡住，可以按 `Ctrl+C` 取消后重试
- 国内网络可能需要使用代理或 VPN

